Take notes for git operation.

And try some features on Sourcetree.

Try commit a file on different host, then check what happened on local Sourcetree:

1. No respond after `add` on other host.
2. Got respond after `commit` on other host.
3.  after `push` on other host.
4. 故本地Sourcetree不能感知外地分机对共有文件的改动，也无法感知远程库对应文件的变化